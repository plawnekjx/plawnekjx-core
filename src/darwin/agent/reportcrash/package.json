{
  "name": "plawnekjx-reportcrash-agent",
  "version": "1.0.0",
  "description": "Agent used by Plawnekjx's Darwin backend on iOS/iPadOS/tvOS",
  "type": "module",
  "scripts": {
    "build": "esbuild index.js --outfile=reportcrash.js --bundle --platform=node --target=es2022 --minify"
  },
  "devDependencies": {
    "esbuild": "^0.25.5"
  },
  "dependencies": {
    "plawnekjx-objc-bridge": "^8.0.5"
  }
}
